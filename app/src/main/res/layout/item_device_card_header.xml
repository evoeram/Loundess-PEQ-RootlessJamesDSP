<?xml version="1.0" encoding="utf-8"?>
<!--
  Заголовок «Другие устройства (N)» — сворачиваемый переключатель.
  Тап разворачивает/сворачивает список неактивных устройств.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingHorizontal="16dp"
    android:paddingVertical="12dp"
    android:clickable="true"
    android:focusable="true"
    android:background="?attr/selectableItemBackground">

    <!-- Заголовок «Другие устройства (N)» -->
    <TextView
        android:id="@+id/header_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textAppearance="?attr/textAppearanceTitleSmall"
        android:textColor="?attr/colorOnSurfaceVariant"
        tools:text="Другие устройства (3)" />

    <!-- Стрелка-индикатор: развёрнуто → вниз, свёрнуто → вправо -->
    <ImageView
        android:id="@+id/header_arrow"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginStart="8dp"
        android:contentDescription="@null"
        app:tint="?attr/colorOnSurfaceVariant" />

</LinearLayout>
